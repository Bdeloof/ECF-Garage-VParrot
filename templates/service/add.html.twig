{% extends 'base.html.twig' %}

{% block title %}Ajouter un service{% endblock %}

{% block body %}
    {{ form_start(serviceForm) }}
        {{ form_row(serviceForm.title) }}
        {{ form_row(serviceForm.description) }}
        {{ form_row(serviceForm.picture) }}
        {{ form_row(serviceForm.price) }}
        <button type="submit" class="btn btn-danger">Ajouter un service</button>
    {{ form_end(serviceForm) }}
{% endblock %}
